"use client";import {useState} from "react";import {Header} from "../../components/header";import {Modal} from "../../components/modal";import {useApp,Material} from "../../components/app-provider";
const blank={title:"",kind:"新闻讲话",direction:"中译阿",topic:"",source:"",original:"",translation:"",aiTranslation:"",notes:""};
export default function Materials(){const {materials,saveMaterial,remove}=useApp();const [open,setOpen]=useState(false),[edit,setEdit]=useState<Material|null>(null);const [form,setForm]=useState(blank);const start=(m?:Material)=>{setEdit(m||null);setForm(m?{...m}:blank);setOpen(true)};return <><Header eyebrow="مكتبة المواد" title="مكتبة المواد" desc="احفظي الأخبار والخطب والمقالات والمقابلات." onAdd={()=>start()}/><div className="content"><div className="grid-2">{materials.map(m=><div className="card" key={m.id}><div style={{display:"flex",justifyContent:"space-between",gap:10}}><span className="tag">{m.kind}</span><span className="tag blue">{m.direction}</span></div><h2 style={{marginTop:15}}>{m.title}</h2><div className="muted" style={{marginTop:7}}>{m.topic||"未分类"} · {m.source||"无来源"}</div><div style={{display:"flex",gap:8,marginTop:18}}><button className="btn secondary" onClick={()=>start(m)}>编辑</button><button className="btn ghost" onClick={()=>remove("materials",m.id)}>删除</button></div></div>)}</div></div>{open&&<Modal title={edit?"编辑材料":"添加材料"} onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={e=>{e.preventDefault();saveMaterial(form,edit?.id);setOpen(false)}}>{[["title","标题"],["topic","主题"],["source","来源"]].map(([k,l])=><div className="field" key={k}><label>{l}</label><input value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<div className="field"><label>类型</label><select value={form.kind} onChange={e=>setForm({...form,kind:e.target.value})}><option>新闻讲话</option><option>外刊文章</option><option>访谈</option><option>纪录片</option><option>其他</option></select></div><div className="field"><label>方向</label><select value={form.direction} onChange={e=>setForm({...form,direction:e.target.value})}><option>中译阿</option><option>阿译中</option><option>双向</option></select></div>{[["original","原文（可选）"],["translation","参考译文（可选）"],["aiTranslation","AI译文（可选）"],["notes","备注（可选）"]].map(([k,l])=><div className="field full" key={k}><label>{l}</label><textarea value={(form as any)[k]||""} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<div className="field full"><button className="btn">保存材料</button></div></form></Modal>}</>}
