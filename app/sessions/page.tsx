"use client";import {useState} from "react";import {Header} from "../../components/header";import {Modal} from "../../components/modal";import {useApp,Session} from "../../components/app-provider";const blank={title:"",direction:"中译阿",minutes:15,date:"",reflection:"",goal:""};export default function Sessions(){const {sessions,saveSession,remove}=useApp();const [open,setOpen]=useState(false),[edit,setEdit]=useState<Session|null>(null),[form,setForm]=useState(blank);const start=(s?:Session)=>{setEdit(s||null);setForm(s?{...s}:blank);setOpen(true)};return <><Header eyebrow="جلسات الترجمة الشفوية" title="تدريبات الترجمة الشفوية" desc="سجّلي كل تدريب وتأملاتك وأهدافك." onAdd={()=>start()}/><div className="content"><div className="card"><div className="list">{sessions.map(s=><div className="list-row" key={s.id}><div><b>{s.title||"تدريب بدون عنوان"}</b><div className="muted">{s.direction} · {s.minutes} دقيقة · {s.date||"بدون تاريخ"}</div></div><div style={{display:"flex",gap:8}}><button className="btn secondary" onClick={()=>start(s)}>编辑</button><button className="btn ghost" onClick={()=>remove("sessions",s.id)}>删除</button></div></div>)}</div></div></div>{open&&<Modal title={edit?"编辑训练":"添加训练"} onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={e=>{e.preventDefault();saveSession(form,edit?.id);setOpen(false)}}><div className="field full"><label>训练标题（可选）</label><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field"><label>方向</label><select value={form.direction} onChange={e=>setForm({...form,direction:e.target.value})}><option>中译阿</option><option>阿译中</option><option>双向</option></select></div><div className="field"><label>时长（分钟）</label><input type="number" value={form.minutes} onChange={e=>setForm({...form,minutes:Number(e.target.value)})}/></div><div className="field"><label>日期</label><input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></div><div className="field full"><label>训练反思（可选）</label><textarea value={form.reflection} onChange={e=>setForm({...form,reflection:e.target.value})}/></div><div className="field full"><label>改进目标（可选）</label><textarea value={form.goal} onChange={e=>setForm({...form,goal:e.target.value})}/></div><div className="field full"><button className="btn">保存训练</button></div></form></Modal>}</>}
