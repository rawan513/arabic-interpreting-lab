 "use client";
import { Header } from "../../components/header";
import { useApp } from "../../components/app-provider";
export default function Tasks(){const {tasks,toggleTask}=useApp();const done=tasks.filter(t=>t.done).length;return <><Header eyebrow="DAILY ROUTINE" title="每日任务" desc={`${done} / ${tasks.length} 项任务已完成`}/><div className="content"><div className="card"><div style={{height:9,background:"#f0eeff",borderRadius:20,overflow:"hidden",marginBottom:22}}><div style={{height:"100%",width:`${done/tasks.length*100}%`,background:"#7567e8",borderRadius:20}}/></div>{tasks.map(t=><div className="list-row" key={t.id}><label style={{display:"flex",gap:13,alignItems:"center",flex:1}}><input type="checkbox" checked={t.done} onChange={()=>toggleTask(t.id)} style={{width:18,height:18,accentColor:"#7567e8"}}/><span style={{textDecoration:t.done?"line-through":"none",fontWeight:700}}>{t.title}</span></label><span className="muted">{t.minutes} 分钟</span></div>)}</div></div></>}
