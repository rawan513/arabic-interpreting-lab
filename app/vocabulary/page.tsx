 "use client";
import { useState } from "react";
import { Header } from "../../components/header";
import { Modal } from "../../components/modal";
import { useApp } from "../../components/app-provider";
export default function Vocabulary(){const {vocab,addVocab}=useApp();const [q,setQ]=useState(""),[open,setOpen]=useState(false);const [form,setForm]=useState({cn:"",ar:"",type:"固定搭配",topic:"",mastery:"不熟悉",example:""});const filtered=vocab.filter(v=>(v.cn+v.ar+v.topic+v.type).includes(q));return <><Header eyebrow="VOCABULARY & EXPRESSIONS" title="词汇表达" desc="积累真正能在口译中使用的表达。" onAdd={()=>setOpen(true)}/><div className="content"><input className="search" placeholder="搜索中文、阿语或主题…" value={q} onChange={e=>setQ(e.target.value)}/><div className="grid-2" style={{marginTop:18}}>{filtered.map(v=><div className="card" key={v.id}><div style={{display:"flex",justifyContent:"space-between",gap:10}}><span className="tag">{v.type}</span><span className={"tag "+(v.mastery==="熟悉"?"green":"orange")}>{v.mastery}</span></div><h2 style={{marginTop:17}}>{v.cn}</h2><div style={{fontSize:20,fontWeight:700,margin:"9px 0",direction:"rtl",textAlign:"left"}}>{v.ar}</div><div className="muted">{v.topic}</div><div style={{marginTop:14,paddingTop:14,borderTop:"1px solid #f0f1f5",fontSize:13,lineHeight:1.8}}>{v.example}</div></div>)}</div></div>{open&&<Modal title="新增词汇表达" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={e=>{e.preventDefault();addVocab(form);setOpen(false);setForm({cn:"",ar:"",type:"固定搭配",topic:"",mastery:"不熟悉",example:""})}}>{[["cn","中文表达"],["ar","阿拉伯语表达"],["topic","主题"],["example","例句"]].map(([k,l])=><div className="field full" key={k}><label>{l}</label><textarea required value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<div className="field"><label>类型</label><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option>固定搭配</option><option>专业术语</option><option>新闻表达</option><option>句型</option></select></div><div className="field"><label>掌握程度</label><select value={form.mastery} onChange={e=>setForm({...form,mastery:e.target.value})}><option>不熟悉</option><option>复习中</option><option>熟悉</option></select></div><div className="field full"><button className="btn">保存词汇</button></div></form></Modal>}</>}
