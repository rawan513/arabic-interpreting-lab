 "use client";
import { useState } from "react";
import { Header } from "../../components/header";
import { Modal } from "../../components/modal";
import { useApp } from "../../components/app-provider";
export default function Errors(){const {errors,addError}=useApp();const [q,setQ]=useState(""),[open,setOpen]=useState(false);const [form,setForm]=useState({type:"术语错误",source:"",mine:"",better:"",reason:"",status:"待复习"});const filtered=errors.filter(e=>(e.source+e.better+e.type).includes(q));return <><Header eyebrow="ERROR BANK" title="错误库" desc="把错误变成下一次进步的提示。 " onAdd={()=>setOpen(true)}/><div className="content"><div style={{display:"flex",gap:10,marginBottom:16}}><input className="search" placeholder="搜索错误、术语或译法…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="table-wrap"><table><thead><tr><th>错误类型</th><th>原文</th><th>我的译法</th><th>推荐译法</th><th>状态</th></tr></thead><tbody>{filtered.map(e=><tr key={e.id}><td><span className="tag orange">{e.type}</span></td><td><b>{e.source}</b><div className="muted">{e.reason}</div></td><td>{e.mine}</td><td>{e.better}</td><td><span className={"tag "+(e.status==="已掌握"?"green":"orange")}>{e.status}</span></td></tr>)}</tbody></table></div></div>{open&&<Modal title="新增错误" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={e=>{e.preventDefault();addError(form);setOpen(false);setForm({type:"术语错误",source:"",mine:"",better:"",reason:"",status:"待复习"})}}>{[["source","原文"],["mine","我的译法"],["better","推荐译法"],["reason","错误原因"]].map(([k,l])=><div className="field full" key={k}><label>{l}</label><textarea required value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<div className="field"><label>错误类型</label><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option>术语错误</option><option>漏译</option><option>误译</option><option>数字错误</option><option>表达不自然</option><option>停顿问题</option></select></div><div className="field"><label>状态</label><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>待复习</option><option>复习中</option><option>已掌握</option></select></div><div className="field full"><button className="btn">保存错误</button></div></form></Modal>}</>}
